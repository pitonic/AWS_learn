# AWS_learn
AWS_learn

# to install simple apache and default page
# 
#
curl -s http://server/path/script.sh | bash /dev/stdin arg1 arg2

curl -s http://server/path/script.sh | bash /dev/stdin arg1 arg2
